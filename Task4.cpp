#include<Iostream>
using namespace std;
int main() {
	//Task 4
	
    bool isitRain = true;
    bool isGroundwet = false;
    
    cout<<"isitRain && isGroundwet = "<< (isitRain && isGroundwet) <<endl;
    cout<<"isitRain || isGroundwet = "<< (isitRain || isGroundwet) <<endl;
    cout<<"!isitRain = "<< !(isitRain) <<endl;
    cout<<"!isGroundwet = "<< !(isGroundwet) <<endl;
	
}